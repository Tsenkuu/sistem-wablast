<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Terjadi Kesalahan - Sistem Penggajian Lazismu Tulungagung</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>
  <body class="bg-gray-100 font-sans">
    <div class="min-h-screen flex items-center justify-center bg-red-50 p-4">
      <div
        class="bg-white p-8 rounded-lg shadow-xl w-full max-w-md text-center"
      >
        <div class="text-red-500 mb-6">
          <i class="fas fa-exclamation-circle text-6xl"></i>
        </div>
        <h1 class="text-3xl font-bold text-red-700 mb-4">Terjadi Kesalahan!</h1>
        <p class="text-gray-700 text-lg mb-6">
          <% if (typeof message !== 'undefined' && message) { %> <%= message %>
          <% } else { %> Maaf, terjadi kesalahan yang tidak terduga. Silakan
          coba lagi nanti. <% } %>
        </p>
        <a
          href="/dashboard"
          class="inline-block bg-blue-600 text-white py-2.5 px-6 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition duration-200 shadow-md"
        >
          <i class="fas fa-arrow-left mr-2"></i> Kembali ke Dashboard
        </a>
        <a
          href="/login"
          class="inline-block bg-gray-600 text-white py-2.5 px-6 rounded-md hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2 transition duration-200 shadow-md mt-4 md:mt-0 md:ml-4"
        >
          <i class="fas fa-sign-in-alt mr-2"></i> Kembali ke Login
        </a>
      </div>
    </div>
  </body>
</html>
